apiVersion: v1
kind: Service
metadata:
  name: ai
  namespace: textile-ai
  labels:
    app: ai
    app.kubernetes.io/name: ai
    app.kubernetes.io/part-of: textile-ai-platform
spec:
  selector:
    app: ai
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
