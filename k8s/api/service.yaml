apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: textile-ai
  labels:
    app: api
    app.kubernetes.io/name: api
    app.kubernetes.io/part-of: textile-ai-platform
spec:
  selector:
    app: api
  ports:
    - name: http
      port: 3001
      targetPort: 3001
      protocol: TCP
  type: ClusterIP
