apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: textile-ai
  labels:
    app: web
    app.kubernetes.io/name: web
    app.kubernetes.io/part-of: textile-ai-platform
spec:
  selector:
    app: web
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
